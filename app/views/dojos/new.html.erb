<h1>New Dojo</h1>
<!-- <% if flash[:errors] %>
    <% flash[:errors].each do |e| %>
        <h3><p> <%= e %> </p></h3>
    <% end %>
<% end %>
we add values to the input so that when the person creates a new dojo and it's invalid, their input doesn't get deleted and the flash messages are still given
refactoring -->
<% if @dojo.errors.any? %>
    <% @dojo.errors.full_messages.each do |e|%>
        <p><%= e %></p>
    <% end %>
<% end %>
<form action="/dojos" method="post">
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <p>
        <label for="branch">Branch:</label>
        <input type="text" name="dojo[branch]" value="<%= @dojo.branch %>">
    </p>
    <p>
        <label for="street">Street:</label>
        <input type="text" name="dojo[street]" value="<%= @dojo.street %>">
    </p>
    <p>
        <label for="city">City:</label>
        <input type="text" name="dojo[city]" value="<%= @dojo.city %>">
    </p>
    <p>
        <label for="state">State:</label>
        <input type="text" name="dojo[state]" value="<%= @dojo.state %>">
    </p>
    <input type="submit" value="Create">
</form>
<a href="/dojos">Go Back</a>